{
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --build src/worker/tsconfig.json",
    "build.watch": "tsc --build --watch src/worker/tsconfig.json",
    "start": "node lib/worker",
    "get.data": "node lib/worker/signalNewSensorData"
  },
  "devDependencies": {
    "@tsconfig/node14": "^1.0.0",
    "typescript": "^4.2.2"
  },
  "name": "hydroponode",
  "dependencies": {
    "temporalio": "^0.3.0"
  }
}
